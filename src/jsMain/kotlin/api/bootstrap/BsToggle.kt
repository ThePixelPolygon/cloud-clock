package api.bootstrap

@Suppress("NAME_CONTAINS_ILLEGAL_CHARS")

@JsName("""/*union*/{collapse: 'collapse', list: 'list', dropdown: 'dropdown'}/*union*/""")
external enum class BsToggle {
    collapse,
    list,
    dropdown
}